# Base URL of the deployed API Gateway / Lambda function URL.
# Leave blank in production if the site and API share an origin behind
# CloudFront. Set to e.g. http://localhost:8000 to point at a local
# backend during `npm run dev`.
VITE_API_BASE=
