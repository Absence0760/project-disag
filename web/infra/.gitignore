.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example
# Plaintext secrets must never land in git; the encrypted file
# (secrets.enc.yaml) is committed by design.
secrets.yaml
secrets.json
!*.enc.yaml
!*.enc.yaml.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
