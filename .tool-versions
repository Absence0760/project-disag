# Toolchain versions for asdf / mise / rtx.
#
# Run `asdf install` (or `mise install`) in this directory to materialise
# everything listed below. The format is `<plugin> <version>` per line.
#
# Pinning a version here is a stronger guarantee than `engines` in
# package.json or `requires-python` in pyproject.toml because asdf shims
# the binary itself.

nodejs 24
python 3.14
terraform 1.15.0
